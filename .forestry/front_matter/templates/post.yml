---
label: post
hide_body: false
fields:
- type: text
  name: title
  label: Title
  description: The title
  config:
    required: true
- type: select
  name: categories
  label: Categories
  description: Select a category
  config:
    required: true
    source:
      type: custom
    options:
        - "Event"
        - "News"
- type: tag_list
  name: tags
  label: Tags
  description: Provide tags for this post
  hidden: [false]
- type: field_group
  name: event
  label: Event Info
  description: Add the location, date and time for the event
  hidden: false
  fields:
  - type: datetime
    name: datetime
    label: Date & Time of Event
    description: The date of the event (if its an event)
  - type: text
    name: location
    label: Location of Event
    description: The location of the event (if its an event)
- type: textarea
  name: feature_text
  label: Feature text
  config:
    wysiwyg: false
    schema:
      format: markdown
pages:
- _posts/2018-08-22-hello-world.md
